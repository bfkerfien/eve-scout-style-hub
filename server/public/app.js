const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function load(){const q=new URLSearchParams({hub:document.getElementById('hub').value,lifetime:document.getElementById('life').value,search:document.getElementById('search').value});const r=await fetch('/api/connections?'+q);const data=await r.json();document.getElementById('rows').innerHTML=data.map(x=>`<tr><td><span class="space">${esc(x.from_system)} → ${esc(x.to_system)}</span></td><td><b>${esc(x.from_system)}</b><small>${esc(x.to_system)}</small></td><td>${esc(x.from_region)}<small>${esc(x.to_region)}</small></td><td>${x.jumps}</td><td><code>${esc(x.signature)}</code></td><td>${esc(x.type)}</td><td>${esc(x.size)}</td><td><span class="life ${x.lifetime.toLowerCase()}">${esc(x.lifetime)}</span></td><td>${esc(x.pilot)}</td><td><span class="status ${x.status.toLowerCase()}">${esc(x.status)}</span></td></tr>`).join('')||'<tr><td colspan="10" class="empty">No connections match the current filters.</td></tr>'}
async function stats(){const s=await (await fetch('/api/stats')).json();document.getElementById('stats').innerHTML=`<div><b>${s.connections}</b><span>ACTIVE CONNECTIONS</span></div><div><b>${s.thera}</b><span>THERA LINKS</span></div><div><b>${s.turnur}</b><span>TURNUR LINKS</span></div><div><b>${s.eol}</b><span>EOL WARNINGS</span></div>`}document.getElementById('search').addEventListener('input',load);document.getElementById('hub').addEventListener('change',load);document.getElementById('life').addEventListener('change',load);stats();load();
